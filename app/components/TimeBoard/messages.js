/*
 * TimeBoard Messages
 *
 * This contains all the text for the TimeBoard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TimeBoard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TimeBoard component!',
  },
});
